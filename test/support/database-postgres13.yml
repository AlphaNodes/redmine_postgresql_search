production:
  adapter: postgresql
  host: postgres
  database: redmine
  username: postgres
  password: postgres
  encoding: utf8
  port: <%= ENV['POSTGRES_PORT'] %>

development:
  adapter: postgresql
  host: postgres
  database: redmine
  username: postgres
  password: postgres
  encoding: utf8
  port: <%= ENV['POSTGRES_PORT'] %>

test:
  adapter: postgresql
  host: postgres
  database: redmine
  username: postgres
  password: postgres
  encoding: utf8
  port: <%= ENV['POSTGRES_PORT'] %>
